#include <SoftwareSerial.h>

int  In1 =3;            //In1 繼電器跟Arduino接的腳位為3

void setup()
{
    pinMode(In1,OUTPUT);       //設置腳位的模式為輸出
}
void loop()
{
    digitalWrite(In1,LOW);   //不通電
    delay(200);

    digitalWrite(In1,HIGH);  //通電
    delay(200);

}
